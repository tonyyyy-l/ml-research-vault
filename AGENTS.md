# Agent Instructions

Use `CLAUDE.md` as the canonical workflow for this repository.

Default vault resolution order:

1. `OBSIDIAN_VAULT_PATH`, if set.
2. `./Research Vault`, relative to this repository root.

When answering ML questions, search the vault first and cite notes with Obsidian links. When editing notes, append only and avoid overwriting user-authored content unless explicitly requested.
